meta {
  name: artist albums
  type: http
  seq: 5
}

get {
  url: http://localhost:4000/api/json/artists/?query=onion&fields=name,album_count,albums
  body: none
  auth: inherit
}

params:query {
  query: onion
  fields: name,album_count,albums
}

settings {
  encodeUrl: true
  timeout: 0
}
